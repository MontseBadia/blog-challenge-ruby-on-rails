<% if post.liked_by_current_user?(current_user) %>
  <button><%= link_to t('posts.index.dislike'), post_likes_path(post.id), method: :delete, remote: true %></button>
<% else %>
  <button><%= link_to t('posts.index.like'), post_likes_path(post.id), method: :post, remote: true %></button>
<% end %>